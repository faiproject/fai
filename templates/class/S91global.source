#! /bin/sh

# these variabel are used by cfengine

chroot=/usr/sbin/chroot
cf_prefix="cfengine:"
files=$FAI_FILES
target=$FAI_ROOT
force=true
backup=true
liloappend=''
kernelimage=KERNELVERSION	# default kernel
hserver=''
bserver=''